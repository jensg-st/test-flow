openapi: 3.0.0
x-direktiv-api: gateway/v1

info:
  title: MyAPI
  description: the description of the API
  version: "1.0"

paths:
  /user:
    $ref: ./route1.yaml

servers:
  - url: http://api.example.com/v1


components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string