x-direktiv-api: endpoint/v2

x-direktiv-path: /mypath  

get:
  summary: Returns a list of users.
  description: Optional extended description in CommonMark or HTML.
  tags:
    - demo-tag
  responses:
    "200":
      description: OK
      content:
        text/plain:
          schema:
            $ref: "#/components/schemas/User"

components:
  schemas:
    User:
      properties:
        id:
          type: integer
        name:
          type: string
